apiVersion: compute.cloud.com/v1
kind: S3Bucket
metadata:
  name: my-app-bucket
spec:
  bucketName: my-app-bucket-kube-provisioner
  region: us-east-1
  versioning: true
  tags:
    env: dev
    managed-by: kubeprovisioner
